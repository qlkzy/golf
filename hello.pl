#!/usr/bin/perl -n

# .- -... -.-. -.. . ..-. --. .... .. .--- -.- .-.. -- -.
# --- .--. --.- .-.  ...  - ..- ...- .-- -..- -.-- --..

#

# .... . .-.. .-.. ---
# .-- --- .-. .-.. -..

BEGIN {$ARGV[0] = $0; $a='a';}
END{print "\n";}

m/^#[^!]/ || next;
s/^#\s*|\s*$//g;
$p ||= m/^$/;

map {$p ? (print $m{$_}) : ($m{$_} = $a++);} (split /\s+/);

print ' ' if $p && $p++ > 1;
